#!/usr/bin/env python

import ibus
bus = ibus.Bus()
ic = ibus.InputContext(bus, bus.current_input_contxt())
if ic.is_enabled():
    print 'iBus disabled'
    ic.disable()
else:
    print 'iBus already disabled'
